console.log("The server is running");
console.log("This is Priya!");

var num = 5;
var length = 4;

function calculateSquare(number)
{
    return number*number;
}
var square = calculateSquare(num);
console.log(`The square of ${num} is ${square}`);

function FindArea(length)
{
    return length*length;
}
var area = FindArea(length);
console.log(`The area of the square with length ${length} is ${area}`);